//! Query execution engine (Phase 4)

use crate::error::Result;

/// Query execution engine - to be implemented in Phase 4
pub struct Executor;

impl Executor {
    pub fn execute(_query: &str) -> Result<()> {
        unimplemented!("Executor - Phase 4")
    }
}
